<%= render partial: "comment", collection: comments %>
<% if has_next %>
    <button data-id ="<%= confession_id %>" data-page = "<%= next_page %>" class="mt-4 bg-blue-500 hover:bg-blue-700 text-white py-2 px-4 rounded" data-action="click->confessions#loadMoreCommentsForConfession">Load More</button>
<% end %>